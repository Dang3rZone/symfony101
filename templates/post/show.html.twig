{% extends 'base.html.twig' %}

{% block body %}
    <h3>{{ post.title }}</h3>
    <em>{{ post.category }}</em>
    <br>
    <img src="{{ '/uploads/' ~ post.image }}" class="img-fluid img-thumbnail" alt="">
{% endblock %}